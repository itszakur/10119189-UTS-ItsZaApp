<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a10119189_myselfapps">
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/logozakurhitam"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logozakurhitam"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quiz">
        <activity android:name=".Profile" />
        <activity android:name=".Gallery" />
        <activity android:name=".Music" />
        <activity android:name=".Daily" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".WalkThrough1"
            android:label="home" />
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>